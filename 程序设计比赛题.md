# 天梯赛 蓝桥杯 ACM

IOI、OI、ACM，都有



## 2024年团体程序设计天梯赛模拟赛（重现赛）

### B《蛋仔派对》高手 

链接：https://ac.nowcoder.com/acm/contest/106441/B
来源：牛客网

**《蛋仔派对》**是网易运营的一款潮玩休闲竞技游戏。

在游戏中，玩家可以变身萌萌的蛋仔，携好友挑战各类奇妙关卡；也可以拾取场景中的道具阻拦其他蛋仔，炸弹、冰冻、电击甚至放屁应有尽有。游戏中还有蛋仔工坊供玩家发挥想象……

 蛋仔派对中的巅峰排队/经典模式中，**竞速赛**是非常经典的一个关卡。

 **竞速赛**中，若本轮共有 323232 个蛋仔参与，那么第 111 名得分为 323232 分，第 222 名得分为 313131 分，第 333 名得分为 303030 分，以此类推，名次每增加 111，分数对应减少 111。

 若本轮有 nnn 个蛋仔参与，第 111 名得分为 nnn 分， 第 222 名得分为 n−1n-1n−1 分，以此类推。贝贝想知道第 kkk 名小蛋仔的得分是多少？

输入描述:

```
仅一行，包含两个整数 n,k(8≤n≤32,1≤k≤n)n,k(8\le n\le 32,1\le k\le n)n,k(8≤n≤32,1≤k≤n) 。
```

输出描述:

```
仅一行，包含一个整数，表示第 kkk 名小蛋仔的得分。
```

示例1                        

输入

```
32 2
```

输出

```
31
```



**Java ACM模式：**

```java
import java.util.Scanner;

// 注意类名必须为 Main, 不要有任何 package xxx 信息
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int k = sc.nextInt();
        int score = n - k + 1;
        System.out.println(score);
    }
}
```



### C 云想衣裳花想容 

链接：https://ac.nowcoder.com/acm/contest/106441/C
来源：牛客网              

​	“**云想衣裳（chang）花想容，春风拂槛露华浓。**”这是《王者荣耀》中**杨玉环**这个英雄的台词之一。

​	**杨玉环**也是贝贝最拿手的一个《王者荣耀》英雄之一，她以极强的拉扯能力和自保能力，常年出现在高分段的中，深受多数中路玩家喜爱。

​	其二技能的加速与眩晕控制，是其拉扯能力的主要来源。杨玉环释放二技能，1.5 秒后对**圆环**范围内的敌人造成眩晕效果。



贝贝想知道这个眩晕的面积是多少，已知内半径为 r_1，外半径为 r_2，请你计算杨玉环二技能的眩晕面积 S=π((r_2)^2 − (r_1)^2) ，其中取圆周率 π=3.141592。

输入描述:

```
仅一行，包含两个整数 r1,r2(0<r1<r2≤100)
```

输出描述:

```
仅一行，表示取圆周率 π=3.141592\pi=3.141592π=3.141592 时，圆环的面积 SSS 的值，答案保留 `6` 位小数。
```

示例1                        

输入

```
2 4
```

输出

```
37.699104
```



**Java ACM模式：**

```java
import java.util.Scanner;

// 注意类名必须为 Main, 不要有任何 package xxx 信息
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        double r1 = sc.nextDouble();
        double r2 = sc.nextDouble();
        
        double area = 3.141592 * (r2*r2 - r1*r1);
        System.out.println(String.format("%.6f", area));
    }
}
```



### D 记忆数字

链接：https://ac.nowcoder.com/acm/contest/106441/D
来源：牛客网

小 π 完成画派挑战后，打算把 π 记下来，但这对于他来说太难了。

 于是小 π找了一些英文单词拼凑一段话，只要记住这段话就可以记住这串数字。

 假设现在有一个单词 s ，这个单词的长度是 l ，那么这个单词对应的数字就是 l 的个位。

 现在小 π 把这段话发给你，你能回答出这串数字是什么吗？

输入描述:

```
第一行输入一个字符串 SSS ，代表小 π\piπ 说的话，字符串包含大小写字母、空格和英文标点符号 ", . ? !"。

相邻两个单词之间存在空格或标点符号分隔，字符串 SSS 的总长度不超过 100001000010000 。
```

输出描述:

```
输出一串数字，表示字符串 SSS 对应的那串数字。
```

示例1                        

输入

```
Wow, I have a smart professor at school. Smart cat often frighten brilliant physics professor.
```

输出

```
314159265358979
```



**Java ACM模式：**

```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String s = scanner.nextLine();
        String[] words = s.split("[^a-zA-Z]+");
        StringBuilder result = new StringBuilder();
        for (String word : words) {
            if (!word.isEmpty()) {
                int len = word.length();
                result.append(len % 10);
            }
        }
        System.out.println(result.toString());
    }
}
/*
下面写法不能AC，只能通过部分测试点
import java.util.Scanner;

// 注意类名必须为 Main, 不要有任何 package xxx 信息
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        StringBuilder sb = new StringBuilder();
        int count = 0;
        for (int i = 0; i < str.length(); i++) {
            char c = str.charAt(i);
            if (c >= 'a' && c <= 'z' || c >= 'A' && c <= 'Z') {
                count++;
            }
            else if (count > 0) { // key
                sb.append(count);
                count = 0;
            }
        }
        if (count > 0) { // key
            sb.append(count);
        }
        System.out.println(sb.toString());
    }
}

*/

```



### E 幻兽帕鲁

链接：https://ac.nowcoder.com/acm/contest/106441/E
来源：牛客网          

“无法抑制爱意的粉丝群”袭击了你的据点……

 成功抵御袭击后，你从小型聚落的村民口中得知，“维克托和助手曾经利用博爱蜥进行基因研究，他们曾经成功研究出异构格里芬，但它与其他帕鲁没有任何基因上的关联，在此之后维克托的助手突然消失，异构格里芬也成为了帕洛斯群岛的谜。”

 为了保护据点和帕鲁，你也开始了基因研究。你发现帕鲁的来源与群岛中心的世界树有关，随着世界树的成长，全新种类的帕鲁也会不断在这个世界中出现，因此这个世界的帕鲁种类数量会受到世界树的影响，每只帕鲁的繁育也会得到世界树的响应。

 在查阅资料和实际验证后，你发现每种帕鲁都具有一个固定的繁育值，并且所有帕鲁的繁育值都是整数，同种帕鲁的繁育值一定相同，不同种帕鲁的繁育值一定不同。

 如果两只繁育值分别为 a, b 的帕鲁进行配种，得到的子代帕鲁的繁育值 c=⌈(a+b)/2⌉，其中 ⌈x⌉ 表示 x 向上取整。

 由于世界树的影响，最终产生的子代帕鲁就是当前世界所有帕鲁中繁育值与子代繁育值 ccc 最接近的那种帕鲁，如果存在两种帕鲁的繁育值都接近 c ，世界树会选择最早在这个世界中出现的帕鲁。

 经过长时间的调查与研究，你终于得到这个世界所有帕鲁的繁育值，并且根据帕鲁在这个世界出现时间从早到晚对帕鲁进行编号。

 现在你想知道，如果选择其中两只帕鲁繁育会得到哪只帕鲁，为了方便配种需要编写一个程序帮助完成这个任务。

输入描述:

```
第一行给出一个正整数 N(1≤N≤500) ，表示这个世界帕鲁的数量。

第二行按照帕鲁出现时间从早到晚依次给出 N 种帕鲁的繁育值，编号为 i(1≤i≤N) 的帕鲁繁育值为 p_i(1≤p_i≤5000)。

第三行给出两个正整数 x,y(1≤x,y≤N)，表示用于配种的两只帕鲁的编号。

同一行中的数字用空格分隔。
```

输出描述:

```
输出一个整数，表示通过编号为 x,y 的帕鲁配种得到子代帕鲁的编号。
```

示例1                        

输入

```
5
10 230 200 500 800
1 5
```

输出

```
4
```



**Java ACM模式：**

```java
import java.util.Scanner;

// 注意类名必须为 Main, 不要有任何 package xxx 信息
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int N = sc.nextInt();
        int[] arr = new int[N];
        for (int i = 0; i < N; i++) {
            arr[i] = sc.nextInt();
        }
        int x = sc.nextInt() - 1; // Convert to 0-based index
        int y = sc.nextInt() - 1;
        int a = arr[x];
        int b = arr[y];
        int c = (a + b + 1) / 2;

        int minDiff = Integer.MAX_VALUE;
        int resultIndex = 0;
        for (int i = 0; i < N; i++) {
            int diff = Math.abs(arr[i] - c);
            if (diff < minDiff) {
                minDiff = diff;
                resultIndex = i;
            }
        }
        System.out.println(resultIndex + 1); // back to 1-based
    }
}

```



### F 统一命名规范

链接：https://ac.nowcoder.com/acm/contest/106441/F
来源：牛客网

你所在的公司刚刚招收了几位程序员，然而这些程序员之前在不同的公司工作，所以他们习惯的变量命名规范可能存在差异，需要让他们都习惯公司要求的命名规范，然而这样可能会降低他们的工作效率。

​	你的上司找到了你，希望你能编写统一命名规范的程序，确保效率不降低的前提下，统一项目变量的命名规范。

​	==现在已经知道公司中只会存在以下三种命名规范：==

​	

- 驼峰（Camel），单词之间没有分隔符，第一个单词首字母小写，其余单词首字母大写，其余字母小写，例如 "userName"； 	
- 帕斯卡（Pascal），单词之间没有分隔符，所有单词首字母大写，其余字母小写，例如 "UserName" ； 	
- 下划线（Snake），单词之间用下划线 "_" 分隔，所有字母小写，例如 "user_name" 。 	



输入描述:

```
第一行输入一个整数 N(1≤N≤100) 和一个字符串 T ，分别表示变量名的数量和最后统一的命名规范，其中命名规范只存在 "Camel"，"Pascal"，"Snake" 三种。

接下来 N 行每行输入一个只包含大小写字母和下划线的字符串 S_i(1≤∣S_i∣≤50) ，表示第 i 个变量名，命名方式为 "Camel"，"Pascal"，"Snake" 中的一种。
```

输出描述:

```
每一行输出一个字符串，表示第 iii 个变量统一成命名规范 TTT 后的变量名。
```

示例1                        

输入

```
4 Snake
userName
Password
background_color
text
```

输出

```
user_name
password
background_color
text
```



**Java ACM模式：**

```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int N = sc.nextInt();
        String T = sc.next();
        sc.nextLine(); // 消耗换行符
        String[] strArr = new String[N];
        for (int i = 0; i < N; i++) {
            strArr[i] = sc.nextLine().trim();
        }

        for (int i = 0; i < strArr.length; i++) {
            String s = strArr[i];
            if (T.equals("Camel")) {
                strArr[i] = toCamel(s);
            } else if (T.equals("Pascal")) {
                strArr[i] = toPascal(s);
            } else { // Snake
                strArr[i] = toSnake(s);
            }
        }

        for (String s : strArr) {
            System.out.println(s);
        }
    }

    // 转换为Camel格式
    public static String toCamel(String s) {
        // 先转换为snake_case
        String snake = toSnake(s);
        StringBuilder sb = new StringBuilder();
        boolean nextUpper = false;
        for (char c : snake.toCharArray()) {
            if (c == '_') {
                nextUpper = true;
            } else {
                if (nextUpper) {
                    sb.append(Character.toUpperCase(c));
                    nextUpper = false;
                } else {
                    sb.append(Character.toLowerCase(c));
                }
            }
        }
        return sb.toString();
    }

    // 转换为Pascal格式
    public static String toPascal(String s) {
        // 先转换为snake_case
        String snake = toSnake(s);
        StringBuilder sb = new StringBuilder();
        boolean capitalizeNext = true;
        for (char c : snake.toCharArray()) {
            if (c == '_') {
                capitalizeNext = true;
            } else {
                if (capitalizeNext) {
                    sb.append(Character.toUpperCase(c));
                    capitalizeNext = false;
                } else {
                    sb.append(Character.toLowerCase(c));
                }
            }
        }
        return sb.toString();
    }

    // 转换为Snake格式
    public static String toSnake(String s) {
        StringBuilder sb = new StringBuilder();
        for (char c : s.toCharArray()) {
            if (Character.isUpperCase(c)) {
                if (sb.length() > 0) {
                    sb.append("_");
                }
                sb.append(Character.toLowerCase(c));
            } else if (c == '_') {
                sb.append(c);
            } else {
                sb.append(c);
            }
        }
        return sb.toString();
    }
}
```